server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ =404;
    }

    location /data/ {
        alias /usr/share/nginx/html/data/;
        autoindex on;  # Allows directory listing to verify files
    }
}
